-- 알림을 눌렀을 때 열 웹 주소.
--
-- V26 에 합치지 않고 따로 둔다. V26 은 로컬 개발 서버가 같은 개발 DB 를 보고 있어
-- 이미 적용됐을 수 있고, 적용된 마이그레이션을 고치면 Flyway 체크섬이 어긋나 기동이 멈춘다.
--
-- 사무처가 붙여 넣은 주소를 그대로 남긴다. 앱으로 보낸 값(경로/바깥 주소)은 이 주소에서
-- 다시 만들 수 있다.

ALTER TABLE push_messages ADD COLUMN link_url VARCHAR(1000) NULL;
